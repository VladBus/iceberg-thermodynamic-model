# DECISIONS.md — Key Decisions Journal

This document explains why the current architecture and physical schemes look
the way they do. Every decision is confirmed by code, tests, and reports; the
historical basis lives in `docs/wiki/`.

Format: `ID | stage/date | status` + short description. Details — via links.

---

## D-01. Real grid reconstructed from IBCAO, not from missing historical files

- **Stage:** 3.5 / 6.1 / 6.2 / 7.6A | **Status:** ACTIVE
- **Problem:** the original `KOORD.DAT`/`hhh.bar` files are lost; the search found nothing.
- **Decision:** the 133×105 grid (132×104 active cells, DX=DY=13.89 km) is reconstructed from IBCAO V5.2 (400 m, EPSG:3996); files are generated by `python/grid/build_real_grid_inputs.py` and symlinked into the root.
- **Why:** the only reproducible bathymetry source; `grid_mode=TEST` is forbidden for production claims.
- **Sources:** `docs/wiki/stages/stage03/Stage3.5_real_grid.md`,
  `docs/wiki/stages/stage07/Stage7.6A_IBCAO_real_grid_reconstruction.md`.
- **Limitations:** cells outside the IBCAO domain are covered by ERA5 (100% coverage since the Stage 7.6C.2 domain expansion; see `docs/wiki/stages/stage07/Stage7.6C.2_ERA5_forcing_expansion_and_hot_run.md`).

## D-02. Mixed unit system: CGS hydrodynamics, SI thermodynamics

- **Stage:** 6.4 / 5.5b | **Status:** ACTIVE
- **Decision:** internally: hydrodynamics in CGS (cm/s, dyn/cm²), thermodynamics in SI (°C, salinity mass fraction); conversion only at the NetCDF output boundary (`netcdf_output.f90`).
- **Why:** legacy of the original Dmitrev–Nesterov model; changing internal units is a separate risky stage.
- **Sources:** `docs/wiki/stages/stage06/Stage6.4_unit_audit.md`,
  `docs/wiki/stages/stage05/Stage5.5b_units.md`.

## D-03. Convective threshold 0.9e-7 is not raised; EOS stays float32

- **Stage:** 4.3 / 4.3b / 4.4 | **Status:** ACTIVE
- **Problem:** convective-adjustment cycling; root cause is float32 quantization `2⁻²³ ≈ 1.19e-7` vs the 0.9e-7 threshold.
- **Decision:** keep threshold and precision; monitor via `ca_reset`/`ca_stats`; 1000-iteration guard. Changing either requires the documented procedure (see `RULES.md`) and approval.
- **Sources:** `docs/wiki/stages/stage04/Stage4.3_convective_root_cause.md`,
  `docs/wiki/stages/stage04/Stage4.4_precision_study.md`.

## D-04. FCT anti-diffusion intentionally disabled

- **Stage:** early | **Status:** ACTIVE
- **Decision:** `CDY*0` in `barotropic_dynamics.f90` (X-block intermediates zeroed).
- **Why:** "fixing" it causes blowup.
- **Sources:** code comments; `AGENTS.md` (Constraints).

## D-05. Ocean freezing point — EOS-80/UNESCO (replaces the linear Zubov law)

- **Stage:** 10.5 | **Status:** ACTIVE
- **Decision:** `Tf(S,p) = (A0 + A1√S − A2·S)·S + BP·P` (Fofonoff & Millard 1983); the literature check value Tf(40 PSU, 500 dbar) = −2.588567 °C is reproduced.
- **Why:** the pressure-dependent formula is physically correct at keel depth; the old Zubov law produced spurious melt below ~8 m.
- **Sources:** `docs/wiki/stages/stage10/Stage10.5_Ocean_Thermal_Forcing.md`,
  `docs/model/model_equation_ledger.md` §4.5–4.7.

## D-06. Three-equation interface with the mass/salt convention correction

- **Stage:** 10.10 / 10.10.1 | **Status:** ACTIVE (selectable; bulk path unchanged)
- **Decision:** H&J99/J2010 scheme (`solve_three_equation_interface`); Eq. III in the mass-conserving form `ρ_w γ_S (S_w − S_B) = ρ_i m S_B` (MOM6/PISM/MITgcm/H&J99 Eq. 4 convention), NOT `γ_S (S_w − S_B) = m S_B` (which implied ρ_i/ρ_w = 1).
- **Effect:** the canonical H&J99 anchor m increased by 10.5%; production end-to-end m by 1.7%.
- **Sources:** `docs/validation/stage10.10_three_equation_interface.md`.

## D-07. Natural convection: formulation retained despite identified limitations

- **Stage:** 10.11 / 10.11.2 / 10.11.3 | **Status:** ACTIVE (selectable), limitations documented
- **Problem:** the Ra cap 1e10 is always active (Nu pinned at 323.17); the haline term is inert; the haline sign is opposite to the physical stabilizing role; zero-flow m = 1.4e-3 m/day is 7–700× below the observed band 0.01–1 m/day.
- **Decision (10.11.3):** production NOT changed (no better alternative without new data); the mechanism is cap-determined; the real mechanism is double-diffusive (MK77/Keitzl16/Middleton21) → motivation for Stage 10.13.
- **Sources:** `docs/validation/stage10.11.3_natural_convection_physics_audit.md`.

## D-08. Prognostic internal temperature — two-node lumped model, fully gated

- **Stage:** 10.12 | **Status:** ACTIVE
- **Decision:** `T_ice` (two nodes: surface + interior), explicit Euler, clamp [−100, 0] °C; switch `thermal_evolution_enabled` (default ON) **fully gates** the stage in the step: OFF = bit-identical legacy (verified by F.1–F.4).
- **Why:** the constant `T_i = −10` was a model assumption; the two-node model is the minimum giving an energy-consistent response without an internal grid.
- **Limitations:** lumped (Bi ≫ 1); excess energy discarded at the clamp; `K_ICE` not calibrated.
- **Sources:** `docs/validation/stage10.12_internal_thermal_evolution.md`, design note alongside.

## D-09. Low-flow closure — research parameterization behind an OFF-by-default switch

- **Stage:** 10.13 (Phases A–C) | **Status:** ACTIVE (research parameterization)
- **Decision:** hybrid "diffusion-limited sublayer + double-diffusive enhancement" integrated into production via `low_flow_closure_enabled` (OFF by default); effective γ_T/γ_S are blended before the existing 3eq solve (not m); the legacy guard `u_rel ≤ 0 → m=0` is preserved via `allow_zero_flow`; NaN fallback to the forced branch.
- **Why:** closes the zero-flow observational gap (0.107 m/day at U=0 vs 1.4e-3), without touching the forced branch (bit-identical at high U).
- **Limitations:** t_scale = 1 day (dominant sensitivity), f_dc = 2.5 not calibrated, κ_S convention (Le=100) is 4.4% below the Python reference.
- **Sources:** `docs/validation/stage10.13_phase_c_results.md`, `docs/validation/stage10.13_phase_b_results.md`, design note.

## D-10. Scalar calibration of the basal-melt coefficient is NOT performed

- **Stage:** 10.9 | **Status:** ACTIVE
- **Decision:** the required C_γ differs by 0.918 orders of magnitude between the two independent sources (NJ80, KW84); the functional-form mismatch (obs ~ dT^1.73 vs closure dT^1.0) is not absorbed by a scalar. Calibration deferred until the three-equation closure (10.10+) and the 10.8.2 re-scoring.
- **Sources:** `docs/validation/stage10.9_calibration_assessment.md`.

## D-11. Switches preserve legacy behavior when OFF

- **Stage:** 10.10–10.13 | **Status:** ACTIVE (mandatory requirement)
- **Decision:** every new physics block enters behind a runtime switch; OFF MUST give a bit-identical previous result (verified by legacy-invariance tests: 10.12 F.1–F.4, 10.13 A.1/D.1).
- **Why:** minimize regression risk and keep old runs reproducible.

## D-12. Unused stdlib dependency removed from fpm.toml

- **Stage:** 10.12 | **Status:** DONE
- **Problem:** `fpm build` compiles only targets reachable from targets; stdlib (0 `use stdlib*` in the repository) broke partial clones and the network build of fpm 0.13.0-alpha.
- **Decision:** dependency removed; clean `build/` before `fpm test`.

## D-13. Real ice initialization: OSI-SAF SIC + C3S CS2SMOS SIT

- **Stage:** 7.6C.1 | **Status:** ACTIVE
- **Decision:** concentration/thickness on 2020-01-01 reconstructed on the model wet mask into two bins preserving area/volume → category files `1_1.ice`–`1_5.ice`.
- **Sources:** `docs/wiki/stages/stage07/Stage7.6C.1_Real_ice_initialization.md`.

## D-14. EN4 ocean initialization: thermal-wind correction and spin-up instead of raw EN4

- **Stage:** 7.7–7.9, 8.0–8.6 | **Status:** ACTIVE
- **Problem:** raw EN4 fields are dynamically incompatible with the zero initial velocity (Stage 7.7B: blowup on day 2, classification C).
- **Decision:** sequence of thermal-wind correction, discrete steady-state solution, and controlled spin-up (Stages 8.2–8.6).
- **Sources:** `docs/wiki/stages/stage07/Stage7.7B_Realistic_Ocean_Stabilization.md`,
  `docs/wiki/stages/stage08/` (8.2–8.6).

---

## D-15. 10.8.2 re-scoring on the 3eq / 3eq+natural closures: verification only, no production change

- **Stage:** 10.14 | **Status:** ACTIVE (verification result)
- **Decision:** the 10.8.2 observational set is re-scored against the already-implemented three-equation (10.10/10.10.1) and three-equation + natural-convection (10.11) closures with the 10.8.2 acceptance criterion. The 3eq closure does NOT improve the u>0 point metrics (RMSE 0.108 → 0.366, bias +0.083 → +0.277; NJ80 functional-form mismatch persists); the 3eq+natural closure reproduces the RH80 quiescent lab rows at the lab scale (5/5 in band) while the iceberg-scale Ra cap keeps the 10.11.3 gap statement valid. No calibration, no production change, no switch-default change.
- **Why:** the roadmap explicitly lists this re-scoring after 10.13; it completes the KNOWN_ISSUES N-05 dependency (calibration remains not identifiable) and qualifies the 10.11.3 gap statement as scale-specific.
- **Sources:** `docs/validation/stage10.14_three_equation_rescoring.md`.

## D-16. Lateral melt — per-iceberg observational bounds; production KEEP_CURRENT

- **Stage:** 10.17 / 10.18A-D | **Status:** ACTIVE
- **Problem:** the legacy lateral closure `m_l = C_LATERAL·⟨ΔT⟩_D`
  (C_LATERAL = 1e-6 m/(s·K)) dominates the melt budget (96.8 % in 10.17) but
  is a documented simplification; 10.18A-D sought observational constraint.
- **Decision:** no production change. Per-iceberg Enderlin23 population
  (743 icebergs, USAP-DC 10.15784/601679 — retrieved 10.18D via direct POST,
  MD5 verified) shows: legacy exceeds 99.6 % (TF = 1.5 °C) / 88 % (TF =
  0.6 °C) of observed per-iceberg melt (median ratio 14×/5.6×); per-iceberg
  C_eff_submarine median = 0.16× production (range 0.08–0.33×); the
  velocity-dependent literature variants bracket observed medians. Velocity
  dependence remains NOT IDENTIFIABLE (no U_ocean in any public dataset);
  the ADCP-equipped Schild21-style campaign design is the velocity-resolved
  prerequisite (10.18D §11).
- **Why:** no formulation is observationally selected at field scale; the
  per-iceberg bounds are now quantitative but cannot discriminate U^0 vs
  U^0.8 closures without paired U_rel. Changing the closure without the
  velocity constraint would be a physics change without evidence.
- **Sources:** `docs/validation/stage10.17_melt_thermodynamic_budget_audit.md`,
  `docs/validation/stage10.18a_lateral_melt_parameterization_audit.md`,
  `docs/validation/stage10.18b_observational_constraint.md`,
  `docs/validation/stage10.18c_existing_observations_reanalysis.md`,
  `docs/validation/stage10.18d_velocity_resolved_observational_upgrade.md`.
- **Limitations:** C_eff_submarine is a SUBMARINE_TOTAL quantity (side+basal)
  vs the model's lateral-only coefficient — comparison is bounded, not a
  direct lateral match; regional TF is representative, not per-iceberg;
  site-level clustering (15 groups) governs statistics, not N = 743.

## How to add decisions

A new decision is appended with an ID, stage, status, and links to reports.
A decision must be confirmed by code/tests/history; unverified assumptions are
not added.
