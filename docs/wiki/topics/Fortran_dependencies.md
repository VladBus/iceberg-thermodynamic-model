# Fortran dependencies

## NetCDF-Fortran

Provided by system package / environment.
Linked through `netcdff`.

## Fortran stdlib

Source:
https://github.com/fortran-lang/stdlib

FPM branch:
stdlib-fpm

Declared in:
fpm.toml

The dependency is downloaded by fpm into:
build/dependencies/

`build/` is generated and is not committed to Git.
