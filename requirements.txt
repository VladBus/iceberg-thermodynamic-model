# Direct dependencies only. The conda environment
# 'iceberg-thermodynamic-model' is the canonical install (see environment.yml).
# Transitive dependencies are resolved automatically by conda/pip.
cdsapi
xarray
netCDF4
numpy
pandas
matplotlib
scipy
pyproj
rasterio